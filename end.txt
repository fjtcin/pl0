var m;
 var *p;        //ok
 var **q;       //ok
 var ***r;      //ok
 var * a[10] ;  //no
 var b[10][10];
 var *c;
 begin
 p := &m;       //no
 q := &p;       //no
 r := &q;       //no
 c:=b[2];       //no
 

 ***r := 100;   //no
 a[0] := &m;    //no
 a[1] := p;     //no
 print(* a[0], *a[1], m);//表达式的值
 end.